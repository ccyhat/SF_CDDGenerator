<UserControl x:Class="SFTemplateGenerator.MainWindow.Views.Controls.PropertyList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SFTemplateGenerator.MainWindow.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Template>
        <ControlTemplate>
            <StackPanel>
                <Border BorderBrush="Black" BorderThickness="1,1,1,0">
                    <DockPanel>
                        <Border BorderBrush="Black" BorderThickness="0,0,1,0" DockPanel.Dock="Left" Width="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=NameColumnWidth}">
                            <TextBlock Text="属性名称" Margin="3" FontSize="24"/>
                        </Border>
                        <Border BorderBrush="Black" BorderThickness="0">
                            <TextBlock Text="属性值" Margin="3" FontSize="24"/>
                        </Border>
                    </DockPanel>
                </Border>
                <Border BorderBrush="Black" BorderThickness="1">
                    <ItemsControl x:Name="_ItemsContainer">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <DockPanel>
                                    <Border BorderBrush="Black" BorderThickness="0,0,1,1" Width="{Binding Column1Width}" DockPanel.Dock="Left">
                                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Margin="3,0,0,0" FontSize="18"/>
                                    </Border>
                                    <Border BorderBrush="Black" BorderThickness="0,0,0,1">
                                        <local:ValueControl Type="{Binding Type}" Value="{Binding Value}"/>
                                    </Border>
                                </DockPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>
            </StackPanel>
        </ControlTemplate>
    </UserControl.Template>
</UserControl>
