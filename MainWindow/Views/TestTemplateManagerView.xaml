<UserControl x:Class="SFTemplateGenerator.MainWindow.Views.TestTemplateManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:cm="http://caliburnmicro.com"
             xmlns:r="clr-namespace:SFTemplateGenerator.MainWindow.Shares"
             xmlns:local="clr-namespace:SFTemplateGenerator.MainWindow.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <DockPanel DockPanel.Dock="Top">
            <Button Content="保存测试模板" DockPanel.Dock="Right" Margin="3">
                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="Click">
                        <cm:ActionMessage MethodName="SaveTestTemplateButtonClicked">
                            <cm:Parameter Value="$source"/>
                            <cm:Parameter Value="$eventArgs"/>
                        </cm:ActionMessage>
                    </b:EventTrigger>
                </b:Interaction.Triggers>
            </Button>
            <Grid/>
        </DockPanel>
        <TabControl HorizontalContentAlignment="Left" DockPanel.Dock="Top"
                    materialDesign:ColorZoneAssist.Mode="PrimaryLight"
                    ItemsSource="{Binding Units}"  SelectedItem="{Binding SelectedUnit}"
                    cm:Message.Attach="[Event SelectionChanged] = [Action TableSelectionChanged($source, $eventArgs)]">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" >
                        <CheckBox IsChecked="{Binding Selected}"/>
                        <TextBlock Text="{Binding Device.Name}"/>
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate/>
            </TabControl.ContentTemplate>
        </TabControl>
        <DockPanel Margin="10,10,10,10">
            <TreeView x:Name="treeView_1" DockPanel.Dock="Left" Width="400" ItemsSource="{Binding SelectedUnit.Tree}"
          cm:Message.Attach="[Event SelectedItemChanged] = [Action SelectedTreeNodeChanged($source, $eventArgs)]"
                      VirtualizingPanel.IsVirtualizing="True">
                <TreeView.Resources>
                    <Style TargetType="TreeViewItem" BasedOn="{StaticResource MaterialDesignTreeViewItem}">
                        <Setter Property="materialDesign:TreeViewAssist.ExpanderSize" Value="16" />
                        <Setter Property="IsExpanded" Value="{Binding IsExpand}"/>
                    </Style>
                    <HierarchicalDataTemplate DataType="{x:Type r:CTreeItem}" ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>

            <ContentControl cm:View.Model="{Binding ActiveItem}"/>
        </DockPanel>
    </DockPanel>
</UserControl>
