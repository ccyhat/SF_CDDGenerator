<UserControl x:Class="SFTemplateGenerator.MainWindow.Views.LoadingWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SFTemplateGenerator.MainWindow.Views"
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Background="Transparent">
    <Grid>
        <Grid x:Name="LoadGrid" Background="Black" Opacity="0.5"/>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Background="White" CornerRadius="5" Padding="20">
                <StackPanel>
                    <TextBlock Text="正在处理数据..." FontWeight="Bold" FontSize="16" HorizontalAlignment="Center"/>
                    <ProgressBar x:Name="IndeterminateToDeterminateCircularProgress" IsIndeterminate="True" 
                          Style="{StaticResource MaterialDesignCircularProgressBar}" Value="50" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
